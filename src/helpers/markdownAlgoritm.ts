import type { MarkdownSyntax } from '../types/markdownTypes';
export const translateMarkdownToHTML = (markdown: string) => {

    const markdownSyntaxOpenList: MarkdownSyntax[] = [];
    
    for(let i = 0; i < markdown.length; ++i) {
        const actualChar = markdown.charAt(i);
    }

}